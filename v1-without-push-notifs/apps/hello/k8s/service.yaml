apiVersion: v1
kind: Service
metadata:
  name: hello
  namespace: mcd
  labels:
    app: hello
spec:
  selector:
    app: hello
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
