apiVersion: v1
kind: Service
metadata:
  name: push
  namespace: mcd
  labels:
    app: push
spec:
  selector:
    app: push
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
